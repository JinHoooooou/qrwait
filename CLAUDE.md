# QR Wait

앱 설치 없이 QR 스캔으로 웨이팅을 등록·관리하는 웹 서비스. 모노레포(`backend/`, `frontend/`).

## 제품 컨텍스트 (작업 판단의 기준)

**이 저장소는 실제 매장에 투입하는 것을 목표로 하는 프로덕션 지향 제품이다. 학습용 토이 프로젝트가 아니다.**

- **목표:** 실제 영업 중인 매장에 QR을 배포해 손님·점주가 실사용하는 서비스. 그 운영 경험을 근거로 제품 완성도를 끌어올린다.
- **품질 기준:** 테이블링·캐치테이블 등 상용 웨이팅 서비스에 준하는 수준. "동작하면 됐다"가 아니라 **실사용자가 매일 쓰는 물건**의 기준으로 판단한다.
- **따라서 다음을 타협하지 않는다:** 데이터 정합성(동시성·채번), 인가·소유권 검증, 운영 환경 설정의 정확성(프로필·마이그레이션), 장애 시 사용자에게 보이는 동작, 테스트.
- 저장소가 `study_study/` 경로 아래 있는 것은 **로컬 디렉터리 사정일 뿐이며 프로젝트 성격과 무관하다.** 학습·연습 프로젝트로 취급하지 않는다.

**판단이 갈릴 때:** "토이 프로젝트니까 이 정도면 충분"이라는 근거로 결정하지 않는다. 대신 *실제 매장 점주가 이 상태로 영업할 수 있는가* 를 기준으로 삼는다. 다만 이것이 오버엔지니어링의 허가는 아니다 — `backend/CLAUDE.md`의 YAGNI 원칙은 그대로 유효하다.

## 작업 전 필독

- **백엔드 코드를 작업·설계·계획할 때는 먼저 [`backend/CLAUDE.md`](./backend/CLAUDE.md)를 읽고 그 아키텍처 규칙을 적용한다.**
  (brainstorming·writing-plans 등 설계 단계에서도 동일하게 먼저 참고한다.)

## 커밋 규칙

- 커밋 메시지·PR 본문에 `Co-Authored-By: Claude ...`, `Claude-Session: ...` 등 Claude 관련 attribution 트레일러를 남기지 않는다.

## 문서 권위 서열 (⚠️ 중요)

저장소에 있는 문서라고 모두 현행 스펙이 아니다. **아래 순서대로 신뢰한다.**

| 순위 | 문서 | 성격 |
|----|------|-----|
| 1 | **코드 자체** | 최종 진실. 문서와 코드가 다르면 코드가 맞다 |
| 2 | `backend/CLAUDE.md` | 현행 백엔드 아키텍처 규칙. 구속력 있음 |
| 3 | `backend/BACKEND_DECISIONS.md` | ADR. **최신 ADR이 이전 ADR을 뒤집을 수 있으므로 끝까지 읽는다** |
| 4 | `docs/superpowers/specs/`, `plans/` 중 **최신 날짜** | 해당 기능의 현행 설계. 파일명 앞의 날짜가 순서다 |
| 5 | `docs/QA-test-cases.md` | 수동 QA 시나리오 |
| — | **`docs/archive/**`** | ❌ **역사 기록일 뿐. 구현 근거로 사용 금지** |

### `docs/archive/` 를 구현 근거로 쓰지 않는다

`docs/archive/`의 PRD/TRD v2.0은 2026-04-03 시점 기록이며 **현행 구현과 여러 곳에서 어긋난다.** 무엇이 어떻게 다른지는 [`docs/archive/README.md`](./docs/archive/README.md)에 정리되어 있다. 아카이브 문서를 참조해야 할 때는 **반드시 그 파일을 먼저 읽는다.**

### 같은 기능에 여러 스펙이 있으면 최신 날짜만 유효하다

예) SMS 알림은 `2026-04-23`(아카이브) → `2026-07-01`(폐기 초안) → **`2026-07-03`(실제 구현)** 순으로 대체됐다. 날짜가 앞선 문서의 설계(예: `shared/notification/` 채널 토글)는 채택되지 않았다.
